name: Backend CI

on:
    push:
        branches: [main]
        paths:
            - 'backend/**'
            - '.github/workflows/backend-ci.yml'
    pull_request:
        branches: [main]
        paths:
            - 'backend/**'
            - '.github/workflows/backend-ci.yml'

jobs:
    build:
        runs-on: ubuntu-latest
        defaults:
            run:
                working-directory: backend
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-python@v5
              with:
                    python-version: '3.12'
                    cache: 'pip'
            - run: pip install -r requirements-dev.txt
            - run: ruff check app.py supabase_store.py ai/online_ai.py tests
            - run: ruff format --check app.py supabase_store.py ai/online_ai.py tests
            - run: python -m compileall .
            - run: python -m unittest discover -s tests
